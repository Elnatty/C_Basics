#include <stdio.h>
#include <stdlib.h>
#include <conio.h>
int main()
{
  /*  
      Tenary Operator!!
      can be used to assign values to variables if said conditions are met or not.
      it can be achieved using this format below....
      any_variable_name = (condition or action) ? assign_this_if_true : assign_this_if_false.
  */


  // --> usecase 1:
  int x;
  // printf("%d", x == 1 ? 10:5);  // print 10 if x==1 or print 5 if x not equal to or (!=) 1.

  // --> usecase 2:
  int x1=60;
  // (x1 > 70) ? printf("Yes") : printf("No");

}